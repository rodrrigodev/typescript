 namespace Geometry {
    export namespace Areas {
         const PI = 3.14
 
         export function circunference(ray: number): number {
             return PI * Math.pow(ray, 2)
         }
     }
 }